import {MessageStyleLogo} from "./gql.messageStyleLogo";
import {String} from "./gql.string";
export type MessageStyles = {
	layout?: String // The message layout
	logo?: MessageStyleLogo // The message logo
}